import {lessons} from "@/lib/lessons";
export default function Archive(){return <main><nav className="nav"><a className="brand" href="/">100 LESSONS</a><span className="muted">ARCHIVE · {lessons.length}/100</span></nav><section className="archiveHead"><div className="eyebrow">THE ARCHIVE</div><h1>Attempts worth<br/>remembering.</h1><p>Not a collection of perfect answers. A record of problems, attempts, evidence, failures and what we learned.</p></section><section className="archiveList">{lessons.map(x=><a className="archiveRow" href={"/lessons/"+x.slug} key={x.id}><span className="archiveNum">#{x.id}</span><span><b>{x.title}</b><small>{x.summary}</small></span><span className="arrow">↗</span></a>)}</section><footer className="footer"><span>{lessons.length} PUBLISHED · {100-lessons.length} TO GO</span><span>Portable by design.</span></footer></main>}